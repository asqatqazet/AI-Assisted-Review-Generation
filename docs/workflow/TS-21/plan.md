# TS-21 plan

## Outcome

Implement student scale-to-zero and production multi-AZ Terraform configurations (`infra/terraform/`), CI/CD GitHub Actions workflow with AWS OIDC authentication (`.github/workflows/deploy.yml`), and operational runbook with recorded rollback drill (`docs/RUNBOOK.md`).

## Public seam

- `infra/terraform/student/`
- `infra/terraform/production/`
- `.github/workflows/deploy.yml`
- `docs/RUNBOOK.md`
